import type { DiscoveryCopy } from '../types';
import { enDiscovery } from './en';

/**
 * Translation scaffolds for Sprint 4+.
 * Keys match English. Replace values with reviewed native copy — no machine translation dumps.
 */
export const taDiscovery: DiscoveryCopy = { ...enDiscovery, language: 'ta' };
export const hiDiscovery: DiscoveryCopy = { ...enDiscovery, language: 'hi' };
export const mlDiscovery: DiscoveryCopy = { ...enDiscovery, language: 'ml' };
